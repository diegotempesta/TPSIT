<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aggiorna Foto Profilo - Societas</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <!-- Navbar inclusa -->
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <h1 class="text-center mb-4">Aggiorna la tua Foto Profilo</h1>

        <!-- Form per aggiornare immagine del profilo -->
        <div class="row justify-content-center">
            <div class="col-md-6">
                <form action="/update-profile" method="POST" enctype="multipart/form-data" class="card p-4">
                    <!-- Input per selezionare un'immagine -->
                    <div class="mb-3">
                        <label for="profileImage" class="form-label">Scegli una nuova foto:</label>
                        <input type="file" name="profileImage" id="profileImage" class="form-control" required>
                    </div>

                    <!-- Pulsante di invio -->
                    <button type="submit" class="btn btn-primary w-100">Aggiorna</button>
                </form>
            </div>
        </div>

        <!-- Link per tornare alla home -->
        <div class="text-center mt-3">
            <a href="/" class="btn btn-link">Torna alla home</a>
        </div>
    </div>

    <!-- Footer incluso -->
    <%- include('partials/footer') %>

    <!-- Script Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
