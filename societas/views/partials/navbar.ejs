<!-- Navbar  -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <!-- Brand/logo -->
        <a class="navbar-brand" href="/">Societas</a>

        <!-- Toggler per dispositivi mobili -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Link di navigazione -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <% if (typeof username !== 'undefined' && username) { %>
                    <!-- Username visualizzato se loggato -->
                    <li class="nav-item">
                        <span class="nav-link"><%= username %></span>
                    </li>

                    <!-- Logout -->
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Logout</a>
                    </li>

                    <!-- Aggiorna immagine profilo -->
                    <li class="nav-item">
                        <a class="nav-link" href="/update-profile">Aggiorna foto profilo</a>
                    </li>
                <% } else { %>
                    <!-- Login e Signup se non loggato -->
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup">Signup</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
